# neoforge

NeoForge loader module (**Workstream 0 stub** — no Minecraft toolchain yet).

Planned:

```bash
./gradlew :neoforge:runClient
./gradlew :neoforge:runServer
./gradlew :neoforge:build
# jar → neoforge/build/libs/spellgems-<version>-neoforge.jar
```

See multi-loader plan Workstreams 1–6 for platform impl and ModDevGradle wiring.
